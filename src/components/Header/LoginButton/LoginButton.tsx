import React from "react";
import cls from './LoginButton.module.css';

const LoginButton = () => {
	return (
		<a href={'1#'} className={cls['loginButton']}>LOGIN</a>
	)
}

export default LoginButton;
